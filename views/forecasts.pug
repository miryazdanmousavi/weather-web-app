extends default 
block content 
    if forecasts.length < 1
        h1(style="text-align:center;") هیچ شهری پیدا نشد.
    else
        .contianer(style="display:flex;height:100vh;margin-top: 20px;")
            each city in forecasts
                div(style="border: 1px solid black; height: fit-content; width: 100px;text-align:center;margin: 0 10px; width: fit-content; padding: 10px;")
                    h1 
                        a(href=`/city/${city.name}`)=city.name 

                    h3 دما در
                    h3 صبح ← #{city.morningTemp}
                    h3 غروب ← #{city.eveningTemp}
                    h3 شب ← #{city.nightTemp}
